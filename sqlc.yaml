version: "2"

sql:
  - engine: "mysql"
    queries: "./sql/queries"
    schema: "./sql/schema"
    gen:
      go:
        package: "db"
        out: "./internal/db/sqlc"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
